let mongoose;_e9d‍.x([["default",()=>_e9d‍.o]]);_e9d‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);

const {Schema} = mongoose;

const PostSchema = new Schema({
  title: String,
  body: String,
  tags: [String],
  publishedDate: {
    type: Date,
    default: Date.now, // 현재 날짜를 기본 값으로 지정.
  }
});

const Post = mongoose.model('Post', PostSchema);
_e9d‍.d(Post);

